---
title: "Plantilla"
tags: ["Python", "AI", "Algebra", "React"]
draft: false
date: 2026-06-16
---

Este es el contenido del primer _post_ de prueba. **Aquí** hay :c[texto en cyan], aquí hay :v[texto en verde], aquí hay :a[texto en amarillo] y aquí hay :p[texto en purple].

# Titulo 1

## Titulo 2

### Titulo 3

---

# Alejandra te :c[amo]

Esto es una prueba — Alejandra cuqui sabrosa.

```python
def mensaje(nombre):
    return f'Hola {} te amo'

print(mensaje('Alejandra'))
```

---

<Toggle title="Pront para crear RoadMap">

```python
def mensaje(nombre):
    return f'Hola {} te amo'

print(mensaje('Alejandra'))

```

</Toggle>

![Flame5](/public/post-uno.webp)

---

La derivada de <Math formula="x^2" /> es <Math formula="2x" />.

<Math formula="\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}" block />

---

Como podemos observar asi de facil es crear un blog con IA

Este link va a [Google](https://www.google.com)

> "La matemática es el lenguaje en el que Dios escribió el universo."
> — Galileo Galilei

---

| DIA      |         Boques          |      Estrucura de tiempo |
| :------- | :---------------------: | -----------------------: |
| Lunes    | Pytho → Agebra → Ingles | 3 Horas / 40 min / 20 in |
| Martes   | Pytho → Agebra → Ingles | 3 Horas / 40 min / 20 in |
| Miecoles | Pytho → Agebra → Ingles | 3 Horas / 40 min / 20 in |
| Jueves   | Pytho → Agebra → Ingles | 3 Horas / 40 min / 20 in |
| Viernes  | Pytho → Agebra → Ingles | 3 Horas / 40 min / 20 in |

---

<Callout type="tip">Este es un consejo útil para recordar.</Callout>

<Callout type="warning">Cuidado con este detalle específico.</Callout>

<Callout type="error">
    Esto puede causar un error si no lo manejas bien.
</Callout>

<Callout type="success">
    ¡Felicidades, completaste el ejercicio correctamente!
</Callout>
---

<Diagram
    code={`
flowchart TD
    A(Datos de entrada) ==> B(Preprocesamiento)
    B --> C(Modelo ML)
    C --> D(Predicción)
    D --> E{¿Correcto?}
    E -->|Sí| F(Deploy)
    E -->|No| G(Reentrenar)
    G --> C
`}
/>

<Diagram
    code={`
    sequenceDiagram
    Cliente->>Servidor: Petición HTTP
    Servidor->>Base de datos: Query SQL
    Base de datos-->>Servidor: Resultado
    Servidor-->>Cliente: Respuesta JSON
    `}

/>

<Diagram
    code={`
    classDiagram
    class Animal {
        +nombre: string
        +hablar(): void
    }
    class Perro {
        +raza: string
        +ladrar(): void
    }
    Animal <|-- Perro
    `}

/>

<Diagram
    code={`
    gitGraph
    commit
    branch feature
    checkout feature
    commit
    commit
    checkout main
    merge feature
    `}

/>

export const data = [
    { epoch: 1, loss: 0.9, accuracy: 0.5 },
    { epoch: 2, loss: 0.7, accuracy: 0.65 },
    { epoch: 3, loss: 0.5, accuracy: 0.75 },
    { epoch: 4, loss: 0.3, accuracy: 0.85 },
    { epoch: 5, loss: 0.15, accuracy: 0.93 },
];

<Chart data={data} xKey="epoch" />

export const curvaAprendizaje = [
    { epoch: 1, train_loss: 0.95, val_loss: 0.97 },
    { epoch: 5, train_loss: 0.72, val_loss: 0.76 },
    { epoch: 10, train_loss: 0.51, val_loss: 0.58 },
    { epoch: 15, train_loss: 0.34, val_loss: 0.42 },
    { epoch: 20, train_loss: 0.21, val_loss: 0.35 },
    { epoch: 25, train_loss: 0.14, val_loss: 0.31 },
    { epoch: 30, train_loss: 0.09, val_loss: 0.29 },
];

<Chart
    data={curvaAprendizaje}
    xKey="epoch"
    type="line"
    height={350}
    lines={[
        { key: "train_loss", color: "#3EBAFF" },
        { key: "val_loss", color: "#ffd700" },
    ]}
/>
